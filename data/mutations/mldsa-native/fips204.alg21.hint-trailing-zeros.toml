schema = "mutation/1"
mutation_id = "mldsa-native:fips204.alg21.hint-trailing-zeros:neutralize"
clause_id = "fips204.alg21.hint-trailing-zeros"
target = "mldsa-native"
target_commit = "41c00dac3f3ce4f578f16aee9f9cd25e6bc05add"
state = "located"
class = "neutralize_predicate"
intent = "Stop rejecting non-zero padding beyond the declared hint count."
kill_mode = "reject_flip"

[[edit]]
path = "mldsa/src/packing.c"
anchor = """
      if (packed_hints[j] != 0)
"""
replacement = """
      if (0 && packed_hints[j] != 0)
"""
occurrences = 1

[postcondition]
positive_vectors_must_pass = true
witness_reason_labels = ["modified signature - hint"]
prediction = """
DIES. The matrix reports 20 isolated violations at r2026-07-31. Registered
before the run.
"""

# The prediction above is left exactly as registered. It was wrong, and editing
# it after the fact would destroy the only thing that makes it a prediction.
outcome = "SURVIVED"
outcome_note = """
The prediction failed and the failure is the finding. "Isolated" means isolated
among the clauses the battery MODELS, and the FIPS 204 commitment hash is
deliberately not modelled. That check is a near-universal backstop for signature
verification: a perturbation of a signature generally changes w1 and therefore
c-tilde, so these cases are still rejected with the trailing-zeros check removed.
Not universal, though: hint-ordering is KILLED on the same corpus, so some
malformed hints do verify once their structural check is removed. It only
moves where they are rejected.

So a COVERED column predicts death only conditionally, while an ABSENT column
predicts survival soundly. See the note at the top of
src/cupel/analysis/violation_matrix.py.
"""
